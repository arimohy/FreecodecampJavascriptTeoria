function alphabeticalOrder(arr) {
  // Only change code below this line

  return arr.sort()
  // Only change code above this line
}

let r=alphabeticalOrder(["a", "d", "c", "a", "z", "g"]);
console.log(r)